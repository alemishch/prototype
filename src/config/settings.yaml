model:
  name: "meta-llama/Meta-Llama-3-8B-Instruct"
  device: "cuda"
  load_in_8bit: false
  torch_dtype: "float16"

api:
  host: "0.0.0.0"
  port: 8000

bot:
  allowed_user_ids: [] 

experiment:
  save_dir: "./experiments/results"