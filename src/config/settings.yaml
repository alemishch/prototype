model:
  name: "/app/local_model" 
  device: "cuda"
  load_in_8bit: false
  torch_dtype: "float16"

api:
  host: "0.0.0.0"
  port: 8000

bot:
  allowed_user_ids: [437643366]

experiment:
  save_dir: "./experiments/results"